
import { PixelData } from '../types';

/**
 * Módulo 29: Aplica optimizaciones finales para exportación (ej. recortar bordes vacíos).
 */
export function optimizeExport(img: PixelData): PixelData {
  // Por ahora devuelve la imagen tal cual; extensible para trim/packing.
  return img;
}
